# Mirror provenance

Mirror of `Ripnrip/Andromeda` `.claude/skills/swift-canon/` at `a193817`
(2026-08-16). Source of truth is the Andromeda repo — when exhibits change
there, resync this mirror. It exists so the DO-NOT-WRITE law has teeth in
Hydra sessions too (Hydra is a separate repo; agents working here load
`.claude/skills` from this checkout).
